[project]
name = "research-agent-platform"
version = "1.0.0"
description = "Autonomous multi-agent research & business-intelligence platform (CrewAI + LangGraph + Ollama)"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 110
