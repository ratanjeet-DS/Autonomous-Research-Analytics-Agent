# Core orchestration
crewai>=0.86.0
crewai-tools>=0.17.0
langchain>=0.3.0
langchain-community>=0.3.0
langchain-ollama>=0.2.0

# RAG / Vector DB
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Crawling (TradingQnA via Discourse JSON API — no key needed)
requests>=2.32.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
# Optional: self-hosted Firecrawl client (set FIRECRAWL_API_URL in .env)
firecrawl-py>=1.6.0

# Frontend
streamlit>=1.40.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
tenacity>=9.0.0
